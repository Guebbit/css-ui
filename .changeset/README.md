# Changesets

This folder stores release notes entries created with `npm run changeset`.

- `npm run changeset`: add a changeset for pending work
- `npm run version-packages`: apply version/changelog updates from pending changesets
- `npm run release`: publish to npm (used by CI release workflow)
