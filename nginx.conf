server {
    # Boilerplate static hosting server for built frontend assets.
    listen 80;
    server_name _;

    # Default directory where Docker image copies compiled site files.
    root /usr/share/nginx/html;
    index index.html;

    # SPA / static-site fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets aggressively
    location ~* \.(js|css|png|svg|ico|woff2?)$ {
        # Boilerplate cache policy for hashed/static assets.
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Compression
    # Keep conservative defaults for broad compatibility.
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        application/json
        application/javascript
        application/xml+rss
        image/svg+xml;
}
